#include <iostream>

int main() 
{
     std::cout << (123*321) << "\n" ; // Gives  Product
     std::cout << (3221/123) << "\n" ; // Gives Quotient
     std::cout << (321/123) << "\n" ; // Gives Remainder
    
     // Placing /n so that each output ends up in new line 
     return 0;
}
